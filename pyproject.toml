[project]
name = "qijue-poem-generator"
version = "0.1.0"
description = "Character-level GRU for seven-character Chinese quatrain generation"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
