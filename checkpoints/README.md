# Checkpoints

Run training to create `gru_best.pt` or `lstm_best.pt` here. A meaningful submission checkpoint must be trained on the downloaded DICAlab seven-character quatrain subset, so this starter package intentionally does not ship a fake pretrained weight file.
